@model TEMSystem.Models.Event

<style>
    /* ===== FONDO ===== */
    .admin-bg {
        position: fixed;
        inset: 0;
        z-index: 0;
    }

    .admin-bg img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: brightness(0.35);
    }

    .admin-overlay {
        position: fixed;
        inset: 0;
        z-index: 1;
        background: rgba(0,0,0,0.65);
    }

    /* ===== CONTENEDOR ===== */
    .admin-container {
        position: relative;
        z-index: 2;
        min-height: 100vh;

        display: flex;
        align-items: center;
        justify-content: center;

        padding: 40px 20px;
    }

    /* ===== CARD GLASS ===== */
    .admin-card {
        width: 100%;
        max-width: 750px;

        border-radius: 25px;
        padding: 35px;

        background: rgba(255,255,255,0.08);
        backdrop-filter: blur(20px);

        border: 1px solid rgba(255,255,255,0.2);

        color: white;
        box-shadow: 0 20px 60px rgba(0,0,0,0.5);
    }

    /* ===== HEADER ===== */
    .admin-title {
        text-align: center;
        font-size: 2rem;
        font-weight: bold;

        margin-bottom: 25px;

        background: linear-gradient(90deg, #00eaff, #ff00c8);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    /* ===== INPUTS ===== */
    .form-control {
        background: rgba(255,255,255,0.07);
        border: 1px solid rgba(255,255,255,0.15);
        color: white;
        border-radius: 12px;
        padding: 12px;
        transition: 0.3s;
    }

    .form-control:focus {
        background: rgba(255,255,255,0.12);
        border-color: #00eaff;
        box-shadow: 0 0 15px rgba(0,234,255,0.4);
        color: white;
    }

    .form-label {
        color: #ddd;
        margin-bottom: 6px;
        font-size: 0.9rem;
    }

    textarea.form-control {
        resize: none;
    }

    /* ===== BUTTON ===== */
    .btn-admin {
        width: 100%;
        padding: 12px;
        border-radius: 30px;
        border: none;

        background: linear-gradient(90deg, #00eaff, #ff00c8);
        color: white;

        font-weight: 600;
        transition: 0.3s;
    }

    .btn-admin:hover {
        transform: scale(1.03);
        box-shadow: 0 0 25px rgba(255,0,200,0.6);
    }

    /* ===== GRID ESPACIADO ===== */
    .form-group {
        margin-bottom: 18px;
    }

    /* ===== MINI HEADER TAG ===== */
    .admin-subtitle {
        text-align: center;
        color: #aaa;
        margin-bottom: 25px;
        font-size: 0.95rem;
    }

    .form-control:focus {
        background: rgba(255,255,255,0.12);
        border-color: #00eaff;
        box-shadow: 0 0 15px rgba(0,234,255,0.4);
        color: white !important;
    }

    input, textarea {
        -webkit-text-fill-color: white;
    }
    /* ===== BUTTON ROW ===== */
    .btn-row {
        display: flex;
        gap: 12px;
        margin-top: 10px;
    }
    .btn-cancel {
        background: rgba(255,255,255,0.15);
    }
</style>

<!-- FONDO -->
<div class="admin-bg">
    <img src="~/images/fondo.gif" />
</div>
<div class="admin-overlay"></div>

<!-- CONTENIDO -->
<div class="admin-container">

    <div class="admin-card">

        <div class="admin-title">Crear Evento</div>

        <div class="admin-subtitle">
            Admin Panel · Eventra Management System
        </div>

        <form method="post">

            <div class="form-group">
                <label class="form-label">Nombre del evento</label>
                <input asp-for="Nombre" class="form-control" placeholder="Ej: Festival de Música Medellín" />
            </div>

            <div class="form-group">
                <label class="form-label">Fecha</label>
                <input asp-for="Fecha" type="datetime-local" class="form-control" />
            </div>

            <div class="form-group">
                <label class="form-label">Ubicación</label>
                <input asp-for="Ubicacion" class="form-control" placeholder="Ej: Plaza Mayor, Medellín" />
            </div>

            <div class="form-group">
                <label class="form-label">Imagen (URL)</label>
                <input asp-for="ImagenUrl" class="form-control" placeholder="https://..." />
            </div>

            <div class="form-group">
                <label class="form-label">Descripción</label>
                <textarea asp-for="Descripcion" class="form-control" rows="4"
                          placeholder="Describe el evento..."></textarea>
            </div>

            <div class="btn-row">

                <button type="submit" class="btn-admin">
                    Guardar Evento
                </button>

                <a href="/Event/Index" class="btn-admin btn-cancel text-center text-decoration-none">
                    Cancelar
                </a>

            </div>
            

        </form>

    </div>

</div>